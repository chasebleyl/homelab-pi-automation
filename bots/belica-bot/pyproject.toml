[project]
name = "belica-bot"
version = "0.1.0"
description = "Discord bot for Predecessor game statistics"
requires-python = ">=3.9"
# Dependencies managed in root pyproject.toml

[tool.setuptools]
packages = ["cogs", "models", "services"]
